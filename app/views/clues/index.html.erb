<a href="logout">Logout</a>
<br>
<% if @clue_num != 9 then %>
    <p>Your team is currently on clue <%= @clue_num %>.</p>
    <p>The address of your current clue is <%= @cur_address %>.<br> Keep in mind that once you are at the location, it will not always easy to find the clue.</p>
    <div class="log_in">
        <%= form_tag(:action => :question_handler, :method => "get") do %>
            <p><%= label_tag(:text_val, "Please enter in your answer for this clue:") %></p> 
            <p>Answer: <%= text_field_tag :answer, nil, :autofocus => true %></p>
            <p><%= submit_tag("Submit") %></p>
        <% end %>
    </div>
<% else %>
    <h3>You have finished the game!</h3>
    <h4>Congrats! Your team rank is: <%= @cur_team.place %>. Your team finished <%= @time_finished_str %>.</h4>
<% end %>
