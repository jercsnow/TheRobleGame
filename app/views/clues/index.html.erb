<% if @clue_num != 11 then %>
    <h3>Your team is currently on clue <%= @clue_num %>.</h3>
    <p>The address of your current clue is <b><%= @cur_address %></b>.<br> Keep in mind that once you are at the correct location, clue will be hidden. This is apart of the challenge.</p>
    <div class="log_in">
        <%= form_tag(:action => :question_handler, :method => "get") do %>
            <p><%= label_tag(:text_val, "Please enter in your answer for this clue:") %></p> 
            <p>Answer: <%= text_field_tag :answer, nil, :autofocus => true %></p>
            <p><%= submit_tag("Submit") %></p>
        <% end %>
    </div>
<% else %>
    <h3>You have finished the game!</h3>
    <h4>Congrats! Your team rank is: <%= @cur_team.place %>. Your team finished <%= @time_finished_str %>. <br>If you are finishing around 9-10 AM there will breakfast waiting for you at the quad!</h4>
<% end %>
